{
    "name": "letnia-kolonia",
    "version": "1.0.0",
    "scripts": {
        "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install --production",
        "start": "node server/server.js"
    }
}
